// The app's user-visible version, in one place (RR-1).
// KEEP THIS IN SYNC with the "version" field in package.json (and, once the
// native apps exist, the version set in Xcode / Android Studio).
export const APP_VERSION = '0.1.0';
